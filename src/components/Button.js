import React from "react";

function Button() {
  return (
    <button
      style={{
        paddingTop: "3px",
        backgroundColor: "blue",
        width: "75px",
        height: "40px",
        justifyContent: "right",
        color: "white",
        fontWeight: "bold",
        border: "none",
        borderRadius: "4px",
        marginTop: "15px",
      }}
    >
      Explore
    </button>
  );
}

export default Button;
